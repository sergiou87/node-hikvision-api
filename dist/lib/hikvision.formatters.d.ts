import { RawCapabilityResponse } from '../responses';
import { StreamCapabilities } from '../types/stream-capabilities.type';
export declare const formatStreamCapabilities: (raw: RawCapabilityResponse) => StreamCapabilities;
