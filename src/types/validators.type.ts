export type StringCurrentOptions = {
  current: string;
  options: string[];
};

export type NumberCurrentOptions = {
  current: number;
  options: number[];
};

export type NumberMinMaxDefault = {
  min: number;
  max: number;
  default: number;
};

export type NumberMinMaxCurrent = {
  min: number;
  max: number;
  current: number;
};
